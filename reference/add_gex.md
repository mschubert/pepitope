# Add gene expression values to variant result

Add gene expression values to variant result

## Usage

``` r
add_gex(res, rec)
```

## Arguments

- res:

  Annotated variants from \`annotate_coding()\`

- rec:

  Sample record from config file

## Value

Annotated variants including gene counts and TPM
