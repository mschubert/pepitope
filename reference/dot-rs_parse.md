# Parse a read structure

Parse a read structure

## Usage

``` r
.rs_parse(read_structures)
```

## Arguments

- read_structures:

  A character string describing the FASTQ read structure

## Value

A \`list\` with sample and construct barcode ranges
