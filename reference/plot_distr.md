# Plot the read distribution across barcodes

Plot the read distribution across barcodes

## Usage

``` r
plot_distr(dset)
```

## Arguments

- dset:

  The \`SummarizedExperiment\` object from \`count_bc\`

## Value

A \`ggplot2\` object with cumulative read distribution plots
